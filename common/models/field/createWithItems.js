module.exports = function (f) {
/*
  f.createWithItems = function (fields, cb) {
    var FieldItems = this.app.models.FieldItems;

    fields.forEach(function (item) {
      item.id = null;
      item.workflowId =
    });
    f.create(fields, function (er, fd) {
      if (er) throw er;

      cb(null, fd);
    });
  };
  f.remoteMethod(
    'createWithItems',
    {
      accepts: [
        {
          arg: 'fields', type: 'array', required: true
        }
      ],
      returns: {root: true}
    }
  );*/
};
